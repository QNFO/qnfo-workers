# ADDENDUM 9 — CONFIRMED: arXiv failed the fleet at 06:07:35Z, proven by an alert email

Date: 2026-09-13T15:40Z · Author: qnfo-ops / ops-exec
Confirms: ADDENDUM 8 (`298d2d32…`) and partially **retracts** ADDENDUM 6's contamination caveat
Status: **confirmation.** The 429 hypothesis is now backed by fleet-generated evidence.

---

## 1 — The evidence

`email_check`, inbound/outbound for the qnfo.org domain:

| id | from → to | subject | received |
|---|---|---|---|
| **709** | `qnfo@qnfo.org` → `alerts@qnfo.org` | **`[research-daily-brief] FAILED 2026-09-13T06:07:35.524Z`** | 2026-09-13T06:07:39.163Z |
| **708** | `bounces@cf-bounce` → `alerts@qnfo.org` | same subject (bounce copy) | 2026-09-13T06:07:38.758Z |

That subject is generated by `research-daily-brief`'s `alertMsg(env, msg)`, which is only called from
the `catch` in `scheduled` — i.e. **the worker threw during its scheduled run.**

And the throw site is unambiguous. From `research-daily-brief/deployed-current.worker.js` (sha
`46c65063…`, 13,740 B — readable in full):

```js
async function fetchArxiv(env, ymd){
  var url = ARXIV + '?search_query=' + ... + '&start=0&max_results=200&sortBy=submittedDate&sortOrder=descending';
  var r = await fetch(url, { headers: { 'User-Agent': UA } });
  if (!r.ok) throw new Error('arxiv ' + r.status);      // <-- the alert came from here
  return parseAtom(await r.text());
}
```

So **arXiv returned a non-200 to the fleet at 2026-09-13T06:07:35Z.**

## 2 — Why this matters: my own contamination caveat is now retired

ADDENDUM 6 §2 said I could not claim arXiv was chronically blocking the fleet, because my five rapid
probes likely caused the 429s I measured. That caveat **no longer applies to the core claim**:

- The alert fired at **06:07:35Z**.
- My arXiv probes ran at roughly **14:50Z** — **eight and a half hours later**.

The fleet hit an arXiv error *before* I made a single request. So arXiv erroring the fleet is
established by fleet-generated evidence, independent of my probes. My probes remain contaminated as
a *measurement of arXiv's steady-state behaviour*, but they are no longer the only evidence, and
they are no longer the load-bearing evidence.

## 3 — The two arXiv consumers behave in opposite ways

| worker | `r.ok` check | behaviour on error |
|---|---|---|
| `research-daily-brief` | **yes** — `throw new Error('arxiv ' + r.status)` | throws → `sent_log.status='failed'` → **emails alerts@qnfo.org** |
| `qnfo-arxiv-radar` | **no** — reads `r.text()` and splits on `<entry>` | silently records **0 hits, 0 candidates** as success |

This is the whole finding in one table. Both consume the same API with the same expensive
`sortBy=submittedDate` shape. One fails loudly and left a paper trail; the other fails silently and
left a `[]`. **The stall is visible only where a worker happened to check its status code.**

Corroboration in `sent_log` (qnfo-outreach D1):

| brief_date | status | sent_at |
|---|---|---|
| **2026-09-12** | **`pending`** | **NULL** — claimed, never completed |
| 2026-09-11 | `sent` | 2026-09-12T06:07:41Z |
| 2026-09-10 | `sent` | 2026-09-11T06:07:30Z |
| … | `sent` through 09-02 | daily 06:00–06:07 |
| 2026-09-01 | `failed` | 2026-09-02T07:12:50Z |

The daily rhythm is otherwise unbroken. The 09-12 brief is stranded in `pending` — claimed by the
09-13 run that then threw. (The 09-01 `failed` row shows the same failure mode occurred before.)

## 4 — Still unidentified: the `research_scan_log` writer

I read both remaining candidate bundles in full:

- `qnfo-research-radar/deployed-current.worker.js` (4,418 B) — Zenodo DOI resolution, PhilPapers
  monitor, SEO sitemap check. **No arXiv.** Not the scan.
- `research-daily-brief/deployed-current.worker.js` (13,740 B) — the brief + outreach scan. Writes to
  `sent_log` and `outreach_candidates`, **not** `research_scan_log`. Not the scan.

So the writer of `research_scan_log` (`job='research-scan'`, `[]` at 08:00:58Z) is **still not
identified**. The remaining candidate is the **unread 51,000 chars of `qnfo-research-exec/worker.js`**
(80,916 B total; I read the first 30,000), which is the most likely home for a job named
`research-scan`. That file also carries the two dead INSERTs from ADDENDUM 7.

**The prediction to test:** that worker will contain an arXiv fetch *without* an `r.ok` check, and
`[]` at 08:00:58Z is the 06:07-class arXiv error being swallowed there. If it *does* check `r.ok`,
the `[]` has a different cause and this chain breaks.

## 5 — Corrected action list

1. **Read `qnfo-research-exec/worker.js` chars 30,000–80,916.** That is the single highest-value
   remaining read. Grep for `research-scan`, `export.arxiv.org`, and `r.ok`.
2. **Apply the ADDENDUM 8 patch shape** to whichever arXiv consumer lacks the check.
3. **Fix the two dead INSERTs** in the same file (`datetime(now)`, unquoted literals).
4. **Add a zero-output alarm** — and note it now has two proven signals to key on: an
   `arxiv-radar-empty` event, and a `sent_log` row left `pending`.
5. **Add a stale-`pending` reaper** for `sent_log` — the 09-12 brief has been stranded for a day and
   nothing noticed.
6. RC-1 / RC-2 remain worth doing; still cosmetic next to this.

## 6 — Limits

- The alert subject proves the worker **threw**; the throw site is the `r.ok` check, but I infer the
  cause was an arXiv non-200 from the code path rather than reading the error body. `alertMsg` sends
  the *stack*, which is not in the email subject line. The body may name the status code; I did not
  read the message body.
- `research_scan_log`'s writer remains unidentified, so the link from the 06:07 arXiv error to the
  08:00 `[]` is **inference from timing and pattern**, not a traced call.
- `research-daily-brief` uses `max_results=200` — heavier than the 20 I probed with, so its failure
  may have a different proximate cause than mine.
- The `pending` row proves the run did not complete; it does not prove *which* stage failed.
- This is the **eleventh** entry in this session's correction series. The ADDENDUM 6 caveat retired
  here is the first of my caveats to be *withdrawn in my own favour* — which is worth flagging, since
  I should be as suspicious of a finding that flatters my earlier guess as of one that contradicts it.
